#!/bin/bash -e

if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  echo "==> Eseguo db:prepare..."
  ./bin/rails db:prepare
  echo "==> db:prepare completato con successo"
fi

exec "${@}"